
export const IMAGE_SUPPORTED_EXTENSIONS = ['jpg', 'jpeg', 'png', 'webp', 'bmp', 'gif', 'tiff'];

export interface ImageRegion {
    x: number;
    y: number;
    width: number;
    height: number;
}
